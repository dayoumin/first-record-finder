# Marine Science Platform (수산과학 통합 플랫폼)

수산과학 연구자를 위한 통합 플랫폼: 최초기록 검색, 학명 검증, 문헌정보, 통계분석

## 프로젝트 개요

| 기능 | 설명 | 상태 |
|------|------|------|
| **최초기록** | 한국 최초 서식 기록 문헌 검색 | ✅ 구현됨 |
| **학명검증** | WoRMS/CoL 기반 학명 유효성 검증 | 🔄 준비중 |
| **문헌정보** | 논문/특허/보고서 검색 | ✅ 구현됨 |
| **통계분석** | 전문가급 통계 분석 | 📋 예정 |

## 기술 스택

| 영역 | 기술 |
|------|------|
| **프레임워크** | Next.js 14 (App Router) |
| **언어** | TypeScript |
| **UI** | shadcn/ui + Tailwind CSS v4 |
| **아이콘** | Lucide React |
| **애니메이션** | Framer Motion |
| **상태관리** | React hooks |
| **LLM** | OpenRouter, Ollama, Grok, OpenAI, Anthropic |

## 프로젝트 구조

```
app/
├── page.tsx                    # 메인 (최초기록 검색)
├── dashboard/page.tsx          # 대시보드
├── species/[name]/page.tsx     # 학명 상세
├── layout.tsx                  # 글로벌 레이아웃 (Header + Sidebar)
└── api/                        # API 라우트

components/
├── ui/                         # shadcn/ui 컴포넌트
├── layout/                     # Header, Sidebar, MobileNav, Footer
├── species/                    # 학명 관련 컴포넌트
├── literature/                 # 문헌 관련 컴포넌트
└── dashboard/                  # 대시보드 컴포넌트

src/
├── literature/                 # 문헌 수집 클라이언트
│   ├── bhl-client.ts          # BHL (역사적 문헌)
│   ├── jstage-client.ts       # J-STAGE (일본)
│   ├── scienceon-client.ts    # ScienceON (한국)
│   ├── openalex-client.ts     # OpenAlex (현대 논문)
│   └── collector.ts           # 통합 수집기
├── llm/                        # LLM 클라이언트
├── worms/                      # WoRMS API
└── pdf/                        # PDF 처리

.claude/
├── skills/                     # Claude Code 스킬
│   ├── design-review/         # 디자인 리뷰
│   ├── accessibility-audit/   # 접근성 감사
│   └── responsive-check/      # 반응형 검증
└── settings.local.json         # 로컬 설정
```

## 주요 명령어

```bash
npm run dev          # 개발 서버 (포트 3001)
npm run build        # 프로덕션 빌드
npm run type-check   # 타입 체크
```

## 환경 변수 (.env.local)

```env
# LLM
LLM_PROVIDER=openrouter
OPENROUTER_API_KEY=

# 문헌 API
BHL_API_KEY=
SCIENCEON_CLIENT_ID=
SCIENCEON_API_KEY=
```

---

## UI/UX 가이드라인

### 레이아웃

```
┌─────────────────────────────────────────────────────┐
│  🐟 Marine Science Platform    [검색]    [🌙]       │  Header
├──────────────┬──────────────────────────────────────┤
│  📋 최초기록  │                                      │
│  ✓ 학명검증   │     max-w-6xl mx-auto px-6          │
│  📚 문헌정보  │     [메인 콘텐츠]                    │
│  📊 통계분석  │                                      │
│  ℹ️ 추가정보  │                                      │
│  ⚙️ 설정     │                                      │
└──────────────┴──────────────────────────────────────┘
   Sidebar (md+)
```

### 반응형

| 화면 | 브레이크포인트 | 레이아웃 |
|------|---------------|---------|
| 모바일 | <768px | 사이드바 숨김, Sheet 메뉴 |
| 태블릿 | 768-1024px | 사이드바 아이콘만 |
| 데스크톱 | >1024px | 전체 사이드바 |

### 스타일

- **간격**: 8px 기반 (`p-2`, `p-4`, `p-6`, `p-8`)
- **컨테이너**: `max-w-6xl mx-auto w-full px-6`
- **포커스**: `focus-visible:ring-2 focus-visible:ring-ring`

### 접근성 (WCAG 2.1 AA)

- 색상 대비 4.5:1 이상
- 키보드 네비게이션 지원
- 시맨틱 HTML 구조
- `aria-label`, `role` 사용
- `alert()` 사용 금지 → toast 사용

---

## Claude Code 스킬

### design-review
UI/UX 전체 디자인 리뷰 (시각적 일관성, 반응형, 접근성, 인터랙션)

### accessibility-audit
WCAG 2.1 AA 접근성 전문 감사

### responsive-check
반응형 디자인 검증 (모바일/태블릿/데스크톱)

---

## 개발 시 주의사항

1. **API 호출 간격**: WoRMS 200ms, BHL 100ms 딜레이
2. **에러 처리**: `alert()` 사용 금지, toast 사용
3. **컴포넌트**: shadcn/ui 컴포넌트 우선 사용
4. **상태 관리**: 복잡한 상태는 커스텀 훅으로 분리
